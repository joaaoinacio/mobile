import { colors } from "../../theme/colors";

export const styles = {
    root: {
        width: '100%',
        justifyContent: "center",
        alignItems: 'center'
    },
    denseRoot: {
        width: '100%',
        justifyContent: "center",
        alignItems: 'center',
        backgroundColor: colors.primary.main
    }
}
